import {
  BorderProps,
  BorderRadiusProps,
  LayoutProps,
  SpaceProps,
  FlexboxProps,
} from 'styled-system';

export interface CheckboxProps
  extends SpaceProps,
    LayoutProps,
    BorderRadiusProps,
    BorderProps,
    FlexboxProps {}
